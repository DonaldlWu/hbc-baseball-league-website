# 資料更新指南 (Data Update Guide)

本文件說明如何更新球員統計資料，適用於每週或每賽季的資料更新作業。

---

## 📋 目錄

1. [快速開始](#快速開始)
2. [詳細步驟](#詳細步驟)
3. [檔案結構說明](#檔案結構說明)
4. [常見問題](#常見問題)
5. [錯誤排除](#錯誤排除)

---

## 🚀 快速開始

### 每週更新流程（5 分鐘）

```bash
# 1. 將新的 CSV 放入 data/raw/
cp ~/Downloads/最新資料.csv data/raw/2026-01-31.csv

# 2. 更新 data.csv 符號連結
ln -sf 2026-01-31.csv data/raw/data.csv

# 3. 執行資料轉換
npm run convert-data

# 4. 檢查轉換結果
git status

# 5. 提交變更
git add data/raw/ public/data/
git commit -m "feat: 更新 2026-01-31 球員數據"
git push
```

---

## 📖 詳細步驟

### Step 1: 準備 CSV 檔案

#### 1.1 下載最新 CSV

從聯盟管理系統或 Google Sheets 下載最新的球員統計資料。

**CSV 檔案格式要求：**
- ✅ 編碼：UTF-8
- ✅ 第 1 行：聯盟統計（會被自動跳過）
- ✅ 第 2 行：欄位標題
- ✅ 第 3 行起：球員資料

**必要欄位：**
```
聯盟編碼, 年份, 所屬球團, 背號, 球員,
打席, 打數, 安打, 一安, 二安, 三安, 全打, 打點, 得分, 四死, 三振, 盜壘成功, 犧打, 壘打數,
局數, 人次, 奪三振, 被安打, 被HR, 失分, 責失, ...
```

#### 1.2 存放 CSV 檔案

**檔案命名規則：**
```
data/raw/YYYY-MM-DD.csv
```

**範例：**
```bash
# 2026 年 1 月 31 日的資料
data/raw/2026-01-31.csv

# 2026 年 2 月 7 日的資料
data/raw/2026-02-07.csv
```

**存放位置：**
```bash
# 將下載的 CSV 複製到專案目錄
cp ~/Downloads/球員統計_20260131.csv data/raw/2026-01-31.csv
```

#### 1.3 更新 data.csv 符號連結

`data/raw/data.csv` 是一個符號連結，指向**最新的 CSV 檔案**。

```bash
# 檢查目前連結指向哪個檔案
ls -la data/raw/data.csv

# 輸出範例：
# lrwxr-xr-x  1 user  staff  16 Jan 24 data.csv -> 2026-01-24.csv

# 更新連結指向最新檔案
cd data/raw/
ln -sf 2026-01-31.csv data.csv
cd ../..

# 驗證連結已更新
ls -la data/raw/data.csv
# lrwxr-xr-x  1 user  staff  16 Jan 31 data.csv -> 2026-01-31.csv ✅
```

---

### Step 2: 執行資料轉換

#### 2.1 轉換 CSV 到 JSON

```bash
npm run convert-data
```

**預期輸出：**
```
🚀 開始轉換 CSV 到 JSON...
✅ 載入 32 個球隊映射
📄 讀取 3273 筆資料
✅ 過濾後 3265 筆有效資料
✅ 2026 年度摘要已生成
✅ 2025 年度摘要已生成
✅ 2024 年度摘要已生成
...
✅ 3265 位球員資料已生成
✅ 32 個球團資料已生成
🎉 轉換完成！
```

#### 2.2 （可選）下載球員照片

如果 CSV 中有新球員且包含照片 URL：

```bash
npm run download-images
```

#### 2.3 一鍵執行（轉換 + 下載照片）

```bash
npm run prepare-data
```

---

### Step 3: 驗證轉換結果

#### 3.1 檢查生成的檔案

```bash
# 檢查哪些檔案被修改
git status

# 預期輸出：
# modified:   data/raw/data.csv
# modified:   public/data/seasons/2026_summary.json
# modified:   public/data/seasons/2026_league.json
# modified:   public/data/players/COL064.json
# ... (更多球員檔案)
```

#### 3.2 驗證 JSON 格式

```bash
# 檢查最新年度摘要
cat public/data/seasons/2026_summary.json | jq '.year, .teams | keys | length'

# 輸出範例：
# 2026
# 32

# 檢查特定球員資料
cat public/data/players/COL064.json | jq '.name, .seasons[0].year'

# 輸出範例：
# "陳重任"
# 2026
```

#### 3.3 本地測試

```bash
# 啟動開發伺服器
npm run dev

# 瀏覽器開啟 http://localhost:3000
# 檢查：
# 1. 賽季頁面是否顯示最新資料
# 2. 球員詳細頁面是否正確
# 3. 統計數據是否合理
```

---

### Step 4: 提交變更

#### 4.1 檢查變更內容

```bash
# 查看變更摘要
git diff --stat

# 查看具體變更（選擇性）
git diff public/data/seasons/2026_summary.json | head -50
```

#### 4.2 提交到 Git

```bash
# 加入所有變更的資料檔案
git add data/raw/ public/data/

# 檢查待提交的檔案
git status

# 建立 commit
git commit -m "feat: 更新 2026-01-31 球員數據

- 更新 2026 賽季球員統計資料
- 資料來源：data/raw/2026-01-31.csv
- 共更新 3265 位球員資料

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>"
```

#### 4.3 推送到遠端

```bash
# 推送到 GitHub
git push origin main

# 或推送到目前分支
git push
```

---

### Step 5: 部署到 Vercel

#### 5.1 自動部署（推薦）

如果已設定 Vercel GitHub 整合，推送後會**自動部署**。

**檢查部署狀態：**
1. 前往 https://vercel.com/your-project
2. 查看 Deployments 頁面
3. 等待部署完成（約 2-3 分鐘）

#### 5.2 手動部署

```bash
# 安裝 Vercel CLI（首次）
npm i -g vercel

# 部署
vercel --prod
```

#### 5.3 驗證線上環境

```bash
# 開啟線上網站
open https://your-project.vercel.app

# 檢查：
# 1. 賽季頁面顯示最新資料
# 2. 球員統計數據正確
# 3. 快取機制運作正常
```

---

## 📁 檔案結構說明

### 輸入檔案

```
data/raw/
├── 2026-01-24.csv     # 1/24 的資料（舊）
├── 2026-01-31.csv     # 1/31 的資料（新）✨
└── data.csv -> 2026-01-31.csv  # 符號連結指向最新
```

### 輸出檔案

```
public/data/
├── seasons/
│   ├── 2026_summary.json    # 2026 賽季摘要（所有球員）
│   ├── 2026_league.json     # 2026 聯盟統計
│   ├── 2025_summary.json    # 2025 賽季摘要
│   └── ...
├── players/
│   ├── COL064.json          # 陳重任完整生涯資料
│   ├── MOR081.json          # 其他球員
│   └── ...（3265 個球員檔案）
└── teams.json               # 所有球團列表
```

### 轉換流程

```
data/raw/data.csv
        ↓
scripts/csv-to-json.ts
        ↓
public/data/
├── seasons/      (每年 2 個檔案：summary + league)
├── players/      (每位球員 1 個檔案)
└── teams.json    (所有球團)
```

---

## 🔧 進階操作

### 批次處理多個 CSV

如果需要同時處理多份 CSV（例如補歷史資料）：

```bash
# 方法 1：逐一處理
for date in 2026-01-24 2026-01-31 2026-02-07; do
  ln -sf ${date}.csv data/raw/data.csv
  npm run convert-data
  git add public/data/
  git commit -m "feat: 更新 ${date} 球員數據"
done

# 方法 2：合併提交
ln -sf 2026-02-07.csv data/raw/data.csv  # 最新的
npm run convert-data
git add data/raw/ public/data/
git commit -m "feat: 更新 2026-02-07 球員數據（含歷史資料）"
```

### 驗證資料完整性

```bash
# 檢查球員數量
cat public/data/seasons/2026_summary.json | jq '[.teams[].players | length] | add'

# 檢查球隊數量
cat public/data/seasons/2026_summary.json | jq '.teams | keys | length'

# 檢查最新更新時間
cat public/data/seasons/2026_summary.json | jq '.lastUpdated'
```

### 清理舊資料

```bash
# 刪除 3 週前的 CSV（保留最近 3 週）
cd data/raw/
ls -t *.csv | tail -n +4 | xargs rm

# 或保留最近 5 份
ls -t *.csv | grep -v data.csv | tail -n +6 | xargs rm
```

---

## ❓ 常見問題

### Q1: 轉換後球員數量變少了？

**可能原因：**
- CSV 格式錯誤（缺少必要欄位）
- 編碼問題（非 UTF-8）
- 資料被過濾掉（year <= 0 或 name 為空）

**解決方法：**
```bash
# 檢查 CSV 編碼
file -I data/raw/data.csv
# 應輸出：charset=utf-8

# 檢查 CSV 行數
wc -l data/raw/data.csv
# 應約 3200+ 行

# 檢查過濾日誌
npm run convert-data | grep "過濾後"
# 輸出：✅ 過濾後 3265 筆有效資料
```

### Q2: 某些球員資料沒更新？

**可能原因：**
- CSV 中缺少該球員的最新資料
- 球員編碼改變

**解決方法：**
```bash
# 搜尋球員在 CSV 中的資料
grep "陳重任" data/raw/data.csv

# 檢查球員 JSON 最後更新時間
cat public/data/players/COL064.json | jq '.seasons[0].year'
# 應為最新年份
```

### Q3: 本地看到新資料，但線上沒更新？

**可能原因：**
- 快取問題（CDN 快取 1 天）
- 部署未完成
- 忘記推送到 GitHub

**解決方法：**
```bash
# 1. 確認已推送
git log origin/main..HEAD
# 如果有輸出 = 還有 commit 未推送

# 2. 推送到遠端
git push

# 3. 等待 Vercel 自動部署（2-3 分鐘）

# 4. 清除瀏覽器快取後重新整理
# 或使用無痕模式測試
```

### Q4: 執行 convert-data 出現錯誤？

**錯誤 1: `Cannot find module 'csv-parse'`**
```bash
# 重新安裝依賴
npm install
```

**錯誤 2: `ENOENT: no such file or directory 'data/raw/data.csv'`**
```bash
# 確認符號連結存在
ls -la data/raw/data.csv

# 如果不存在，建立連結
cd data/raw/
ln -s 2026-01-31.csv data.csv
cd ../..
```

**錯誤 3: `SyntaxError: Unexpected token`**
```bash
# CSV 格式錯誤，檢查檔案
head -5 data/raw/data.csv

# 確認：
# - 第 1 行：聯盟統計
# - 第 2 行：欄位標題（包含：聯盟編碼, 年份, 球員...）
# - 第 3 行起：球員資料
```

---

## 🛠️ 錯誤排除

### 除錯步驟

#### 1. 檢查 CSV 檔案

```bash
# 檢查檔案存在
ls -lh data/raw/data.csv

# 檢查檔案編碼
file -I data/raw/data.csv

# 檢查前 10 行
head -10 data/raw/data.csv

# 檢查欄位數量（第 2 行）
head -2 data/raw/data.csv | tail -1 | awk -F',' '{print NF}'
# 應 > 50 個欄位
```

#### 2. 檢查腳本執行

```bash
# 詳細輸出模式
DEBUG=* npm run convert-data

# 或直接執行
npx tsx scripts/csv-to-json.ts
```

#### 3. 檢查輸出檔案

```bash
# 檢查最新年度檔案
ls -lt public/data/seasons/ | head -5

# 驗證 JSON 格式
cat public/data/seasons/2026_summary.json | jq empty
# 無輸出 = JSON 格式正確

# 檢查檔案大小
du -sh public/data/seasons/2026_summary.json
# 應 > 200KB
```

#### 4. 回復到上一版本

如果轉換出錯，可以回復：

```bash
# 放棄所有變更
git checkout -- public/data/

# 或回復到上一個 commit
git reset --hard HEAD~1

# 重新轉換
npm run convert-data
```

---

## 📅 建議的更新週期

| 時機 | 頻率 | 說明 |
|------|------|------|
| **賽季進行中** | 每週一次 | 週一更新上週比賽數據 |
| **賽季結束** | 一次 | 更新最終統計資料 |
| **年度資料** | 每年一次 | 新賽季開始前整理舊資料 |

---

## 📝 Checklist

每次更新資料時，請確認：

- [ ] CSV 檔案命名正確（`YYYY-MM-DD.csv`）
- [ ] CSV 檔案編碼為 UTF-8
- [ ] 更新 `data.csv` 符號連結
- [ ] 執行 `npm run convert-data` 成功
- [ ] 檢查轉換輸出無錯誤
- [ ] 本地測試資料顯示正確
- [ ] 提交變更到 Git
- [ ] 推送到 GitHub
- [ ] 確認 Vercel 自動部署成功
- [ ] 驗證線上環境資料正確

---

## 🔗 相關文件

- [CSV 轉 JSON 腳本說明](../scripts/csv-to-json.ts)
- [API 快取策略](./api/cache-strategy.md)
- [專案開發指南](../claude.md)

---

## 📞 支援

如有問題，請：
1. 查看本文件的「常見問題」章節
2. 查看「錯誤排除」章節
3. 聯繫開發團隊

---

**最後更新：** 2026-01-24
